import {DerivedUnit} from "./derived-unit";

export class Sievert extends DerivedUnit {
	public readonly symbol: string = "Sv";
	public readonly quantity: string = "equivalent dose (of ionising radiation)";
}