import {DerivedUnit} from "./derived-unit";

export class Sievert extends DerivedUnit {
	public readonly symbol: 'Sv' = 'Sv';
	public readonly quantity: 'equivalent dose (of ionising radiation)' = 'equivalent dose (of ionising radiation)';
}