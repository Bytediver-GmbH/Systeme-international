import {DerivedUnit} from "./derived-unit";

export class Ohm extends DerivedUnit {
	public readonly symbol: 'Ω' = 'Ω';
	public readonly quantity: 'resistance, impedance, reactance' = 'resistance, impedance, reactance';
}