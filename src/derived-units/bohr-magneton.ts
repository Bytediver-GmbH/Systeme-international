import {DerivedUnit} from "./derived-unit";

export class BohrMagneton extends DerivedUnit {
	public readonly symbol: string = "μB";
	public readonly quantity: string = "magnetic moment of an electron caused by either its orbital or spin angular momentum";
}