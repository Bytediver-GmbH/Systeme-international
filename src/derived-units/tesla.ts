import {DerivedUnit} from "./derived-unit";

export class Tesla extends DerivedUnit {
	public readonly symbol: 'T' = 'T';
	public readonly quantity: 'magnetic flux density' = 'magnetic flux density';
}