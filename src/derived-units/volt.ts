import {DerivedUnit} from "./derived-unit";

export class Volt extends DerivedUnit {
	public readonly symbol: 'V' = 'V';
	public readonly quantity: 'voltage (electrical potential), emf' = 'voltage (electrical potential), emf';
}