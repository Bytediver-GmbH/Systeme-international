import {NonSiUnit} from "./non-si-unit";

export class Degree extends NonSiUnit {
	public readonly symbol: '°' = '°';
	public readonly quantity: 'plane angle (dimensionless unit)' = 'plane angle (dimensionless unit)';
}