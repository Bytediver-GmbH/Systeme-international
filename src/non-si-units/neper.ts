import {NonSiUnit} from "./non-si-unit";

export class Neper extends NonSiUnit {
	public readonly symbol: 'Np' = 'Np';
	public readonly quantity: 'level of a field quantity' = 'level of a field quantity';
}